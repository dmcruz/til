# get hostname from ip address

## Solution

### Using nslookup

For looking up the DNS name

```
nslookup X.X.X.X
```

### Using nbtstat

For retrieving Net BIOS name or the PC name
```
nbtstat -a X.X.X.X
```

